import XCTest
@testable import SearchCharge

final class SearchChargeTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SearchCharge().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
